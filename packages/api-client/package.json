{
  "name": "@burnfront/api-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed API client. types.gen.ts is GENERATED from contracts/openapi.yaml (openapi-typescript) — never hand-edited; client.ts is a thin zero-dependency fetch wrapper type-locked to the generated paths.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --coverage",
    "generate": "node scripts/generate.mjs",
    "generate:check": "node scripts/generate.mjs --check"
  },
  "dependencies": {},
  "devDependencies": {
    "openapi-typescript": "^7.10.1"
  }
}
